// The cat lesson's finished drawing rendered cold (no replay cache, no schedule): the identity gate's other half.
import { lessonCat } from "./lessonCat";

export const lessonCatFinal = lessonCat.final;
